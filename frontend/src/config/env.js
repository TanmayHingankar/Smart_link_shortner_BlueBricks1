export const env = {
  // Backend is mounted at `/api` (see backend app.js)
  // Update if your backend runs elsewhere.
  apiBaseUrl: import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000',
  // Socket/other future envs can go here.
}

